---
  - name: Shell Examples
    hosts: all
    tasks:

    - name: Borrar Onpremise TM
      shell:
         "/opt/ds_agent/dsa_control -r"
      register: datecmd
      tags: datecmd
    - debug: msg="{{datecmd.stdout}}"
